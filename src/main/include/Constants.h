#pragma once

#define OVERUNDER

constexpr double kMinOut = -1.0;
constexpr double kMaxOut = 1.0;

constexpr double kOverMinOut = -1000.0;
constexpr double kOverMaxOut = 1000.0;

constexpr double kUnderMinOut = -1000.0;
constexpr double kUnderMaxOut = 0.0;

constexpr double kBackMinOut = -1000.0;
constexpr double kBackMaxOut = 0.0;

constexpr double kIntakeDeployGearRatio = 22.0 / 36.0;  // 22T sprocket input, 36T sprocket output
